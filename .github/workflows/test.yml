name: test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install yq
      shell: bash
      run: |
        sudo snap install yq

    - name: Load environment
      shell: bash
      run: |
        yq '. | to_entries | map([.key, .value] | join ("=")) | .[]' "env.yml" >> "${GITHUB_ENV}"
        echo "env.my_var='${{ env.my_var }}'"
